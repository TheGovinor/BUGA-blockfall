using UnityEngine;
using System.Collections;

public class Player : MonoBehaviour
{
	int lives = 3;
	bool[] keys = new bool[7000];

	// Use this for initialization
	void Start ()
	{
	
	}
	
	// Update is called once per frame
	void Update ()
	{
		//Horizontal keys (a,d)
		if(Input.GetKey("fire1")){

		}

		//if(Input.GetAxis()){



	}
}

